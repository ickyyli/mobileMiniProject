name: kindisync
description: "A new Flutter project."
publish_to: 'none' 

version: 1.0.0+1

environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8

  # Firebase dependencies
  firebase_core: ^3.8.0
  firebase_auth: ^5.3.3
  cloud_firestore: ^5.5.0
  firebase_storage: ^12.3.6

  # Pakej Tambahan
  image_picker: ^1.1.2
  intl: ^0.19.0
  qr_flutter: ^4.1.0
  mobile_scanner: ^7.2.0
  firebase_messaging: ^15.2.10

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  # Pastikan jika anda guna imej tempatan (local assets), un-comment di bawah:
  # assets:
  #   - assets/images/